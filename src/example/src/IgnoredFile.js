Namespace.define({
    name: "IgnoredFile",
    definition: function () {
        var IgnoredFile = function IgnoredFile() {};

        console.log("I am an example of a file that gets ignored by the compiler.")

        return IgnoredFile;
    }
});